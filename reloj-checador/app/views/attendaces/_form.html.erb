<% time = Time.new %>
<%= time.strftime("%I:%M:%S") %>
<%= time.strftime("%Y-%m-%d") %>
<%= form_with(model: attendace) do |form| %>
  
  <div class="field">
    <%= form.label :private_number %>
    <%= form.text_field :private_number %>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
