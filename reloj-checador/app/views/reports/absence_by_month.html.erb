<div class="continer align-content-center ml-1 ml-sm-0 h-100" style="margin-top: 2rem;">
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-12 col-sm-12 col-md-10">
            <h4 class="justify-content-center align-items-center">Ausencias por mes</h4>
        </div>
        <div class="col-12 col-sm-12 col-md-10">
            <br>
            <div class="table-responsive">
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th style="text-align: center;">Año</th>
                            <th style="text-align: center;">Mes</th>
                            <th style="text-align: center;">Compañía</th>
                            <th style="text-align: center;">Entradas Registradas</th>
                            <th style="text-align: center;">Ausencias</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @abscenes.each do |abscene| %>
                            <% company = @companies.select{|company| company if company.id == abscene["company_id"]}.first %>
                            <tr>
                                <td style="text-align: center;"><%=  abscene["yyyy"].to_s.split(".")[0] %></td>
                                <td style="text-align: center;"><%=  abscene["mm"] %></td>
                                <td style="text-align: center;"><%=  company.name %></td>
                                <td style="text-align: center;"><%=  abscene["emp_count"].to_s.split(".")[0] %></td>
                                <td style="text-align: center;"><%=  abscene["abs_count"].to_s.split(".")[0] %></td>
                            </tr>
                        <% end %>
                    </tbody>
                </table>
                <div class="media mb-4">
                    <div class="media-body">
                        <%= will_paginate @abscenes %>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-12 col-sm-12 col-md-10">
            <div class="form-group">
                <form action="<%= reports_absence_by_month_path(format: :xlsx) %>" method="get">
                    <button class="btn btn-success btn-sm" type="submit" style="color: #ffffff;">Descargar CSV</button>
                </form>
            </div>
        </div>
    </div>
</div>
